<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="backgroundColor">@color/white</item>
        <item name="textColor">@color/black</item>
        <item name="hintColor">@color/colorAccent</item>
        <item name="buttonColor">@color/colorFab</item>
        <item name="cardBackgroundColor">@color/card_bg_color</item>
        <item name="android:titleTextColor">@color/title_text_color</item>
    </style>

    <style name="DarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="backgroundColor">@color/black</item>
        <item name="textColor">@color/content_text_color</item>
        <item name="titleTextColor">@color/title_text_color</item>
        <item name="hintColor">@color/colorAccent</item>
        <item name="buttonColor">@color/colorFab</item>
        <item name="cardBackgroundColor">@color/card_bg_dark_color</item>
        <item name="android:titleTextColor">@color/title_text_color</item>
    </style>

    <style name="SplashScreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
</resources>
